import React from "react";
import PropTypes from "prop-types";

const Header = () => (
  <div className="header-container">
    <p className="header__paragraph">There will be a header</p>
  </div>
);

Header.propTypes = {};

export default Header;
